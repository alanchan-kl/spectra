# Login journey (happy path). The login ACTION lives in the reusable subflow;
# this flow supplies the default (valid) creds and asserts the successful outcome.
appId: ${APP_ID}
tags:
  - login
---
- runFlow: ../subflows/login.yaml
- assertVisible: "Products" # successful login returns to the catalog
- takeScreenshot: login-success
